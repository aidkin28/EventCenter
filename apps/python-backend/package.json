{
	"name": "@chat-assistant/python-backend",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "uv run uvicorn assistant.main:app --reload --port 8000",
		"install": "uv sync",
		"lint": "uv run ruff check .",
		"format": "uv run ruff format ."
	}
}
