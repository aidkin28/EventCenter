{
	"name": "chat-assistant-monorepo",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter event-app dev",
		"dev:all": "pnpm generate:types && concurrently -n next,python -c blue,green \"pnpm dev\" \"pnpm python:dev\"",
		"build": "pnpm --filter event-app build",
		"start": "pnpm --filter event-app start",
		"lint": "pnpm -r lint",
		"db:push": "pnpm --filter event-app db:push",
		"db:generate": "pnpm --filter event-app db:generate",
		"db:sync": "pnpm --filter event-app db:sync",
		"db:studio": "pnpm --filter event-app drizzle-kit studio",
		"python:dev": "cd apps/python-backend/src && dotenv -e ../../../.env -v PYTHONPATH=. -- uv run uvicorn assistant.main:app --reload --reload-dir assistant --port 8000",
		"python:install": "cd apps/python-backend && uv sync",
		"generate:types": "tsx scripts/generate-types.ts"
	},
	"devDependencies": {
		"concurrently": "^9.1.2",
		"dotenv": "^16.4.7",
		"dotenv-cli": "latest",
		"tailwindcss": "catalog:",
		"tw-animate-css": "^1.2.4",
		"tsx": "catalog:",
		"typescript": "catalog:"
	}
}
